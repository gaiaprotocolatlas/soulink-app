import { View, ViewParams } from "skydapp-common";
export default class SoulView extends View {
    private container;
    private background;
    private pfp;
    private name;
    private introduce;
    private linkList;
    constructor(params: ViewParams);
    changeParams(params: ViewParams): void;
    private load;
    close(): void;
}
//# sourceMappingURL=SoulView.d.ts.map